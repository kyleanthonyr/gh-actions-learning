name: 01-building-blocks

on: push

jobs:
    echo-hello:
        runs-on: ubuntu-latest
        steps:
            - name: Say hello
              run: echo "Hello world!"

    echo-goodbye:
        runs-on: ubuntu-latest
        steps:
            - name: failed step
              run: |
                  echo "I will fail"
                  exit 1
            - name: say goodbye
              run: |
                  echo "goodbye"
